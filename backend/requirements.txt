# Runtime dependencies -- what JARVIS needs to actually run.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
sqlalchemy==2.0.51
alembic==1.19.0
anthropic==0.120.2
# Used directly by the Ollama adapter (Anthropic's SDK also depends on it).
httpx==0.28.1
# Embedded vector database for long-term memory. Heavy (pulls onnxruntime and
# friends, ~300MB) but runs fully local with no server to manage.
chromadb==1.5.9

# Voice (Phase 7). faster-whisper runs Whisper on CPU via CTranslate2;
# piper-tts is a local neural voice. Both fully offline after first download.
faster-whisper==1.2.1
piper-tts==1.6.0
